# Base URL of the Go API.
#
# Local dev — Vite proxies /api to :8080:
#   VITE_API_BASE_URL=/api/v1
#
# Vercel production — full backend URL (set in Vercel → Environment Variables):
#   VITE_API_BASE_URL=https://api.yourdomain.com/api/v1
VITE_API_BASE_URL=/api/v1
