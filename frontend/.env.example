# Base URL of the Go API.
#
# Local dev — Vite proxies /api to :8080:
#   VITE_API_BASE_URL=/api/v1
#
# Vercel production — keep /api/v1 (vercel.json proxies to EC2 backend):
#   VITE_API_BASE_URL=/api/v1
VITE_API_BASE_URL=/api/v1
