# env
.env
.env.local

# build artifacts (root binary only — do not use bare "api" or cmd/api/ is ignored)
/bin/
/api
*.exe

# uploads (local storage)
/uploads/*
!/uploads/.gitkeep

# go
*.test
*.out
